function plusMinus(arr){
    let pos = 0;
    let neg = 0;
    let zero = 0 ;

    for (1= 0 ; i < arr.length; i++){
        if (arr[1] >0){
            post+=1;}
    else if (arr[1]< 0){
        neg+=1;
    }
    else{
        zero +=1;
    }
    }
    print(pos/arr.length);
    print(neg/arr.length);
    print(zero/arr.length);
    
    


}